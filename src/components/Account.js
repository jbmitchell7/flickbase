import React from 'react';
import PropTypes from 'prop-types';


const Account = () => (
  <div>
    Account Component
  </div>
);

Account.propTypes = {};

Account.defaultProps = {};

export default Account;
